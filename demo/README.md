# PentNote Demo Vault

Run the setup script to create a working demo vault from bundled test fixtures:

```bash
cd demo
bash setup.sh
```

Then open `demo-vault/` in Obsidian.

View the generated HTML report at:

```text
demo-vault/reports/pentnote-report.html
```

Screenshots in `screenshots/` are placeholders for future documentation images.
